import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  mainContainer: {
    display: 'flex',
    marginHorizontal: 10,
  },
  boardButtonContainer: {
    display: 'flex',
    flexDirection: 'row',
  },
});
